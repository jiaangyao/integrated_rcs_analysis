defaults:
  - _self_
  - channel_options: correct_by_channel # Can be Null (i.e. None-type)


strategy:
  imblearn.over_sampling.RandomOverSampler:
    sampling_strategy: 'auto'
    random_state: ${random_seed}