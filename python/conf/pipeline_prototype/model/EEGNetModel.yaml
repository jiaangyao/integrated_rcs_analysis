model_name: EEGNetModel
parameters:
    # chunk_size: int = 151,
    # num_electrodes: int = 60,
    # in_channels: int = 1,
    # F1: int = 8,
    # F2: int = 16,
    # D: int = 2,
    # num_classes: int = 2,
    # kernel_1: int = 64,
    # kernel_2: int = 16,
    # dropout: float = 0.25