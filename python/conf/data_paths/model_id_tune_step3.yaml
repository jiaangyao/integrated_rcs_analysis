defaults:
  - model_id_step3
  - _self_

# overwrite the output directories
p_output: "/home/jyao/Downloads/biomarker_id/tune/${patient.str_subject}"
f_output: "${patient.str_subject}_${patient.str_side}_${meta.label_type}_${meta.str_metric}_${meta.str_model}_tune.pkl"