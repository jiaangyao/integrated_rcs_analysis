defaults:
  - default_no_logging

# loguru info
bool_use_loguru: True

# WandB info
bool_use_wandb: True
wandb_params:
  project: ${setup.project}
  entity: cnsmyth
  group: ${setup.experiment}
  tags: 
    - sleep 
    - ${setup.device}
    - ${setup.run_name}